#ifndef RANK_EXTERNAL_H
#define RANK_EXTERNAL_H

#include <stdbool.h>

#include "options.h"

void rank_external_module_present(void);
bool rank_external_sort(const struct rank_options *options);

#endif
